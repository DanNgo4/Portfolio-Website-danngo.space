import { useEffect } from "react";

const PortfolioPage = () => {
    useEffect(() => {
        document.title = "My Projects";
    }, []);

    return (
        <main>
        
        </main>
    );
};

export default PortfolioPage;